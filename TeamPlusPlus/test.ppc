program Main;

globals g1, g2, g3 int; t1, t3 float;


func foo(cat int, dog float) -> int {
    if x == 7 {
        print(cat);
    } else {
        print(dog);
    }

    var x int;
    var x int = 7 + 10;
    x = 19 + 10;
}

func bar() -> void {
    x = 9;
    while x < 20 || x > 15 {
        x = x + 1;
    }
}

func bar(g1 int, g2 int) -> float {
    return 10;
}

main () {
    x = 5;
    b = 6 + 10;
    c = foo(x, b);
    d = (70 + 10) * 90 + 60 + (80);
    z = c;
    f = b * c + d;
    print(hola);
    input(adios);
    if 2 < 5 { }
    if 2 < 5 && d > 10 {

    } else {

    }

    while 2 < 5 { }

    foo(5,7);
    bar();
    bar(b, foo(7,8));
}

