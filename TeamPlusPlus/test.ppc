program Main;

globals g1, g2, g3 int; t1, t3 float;


func foo(cat int, dog float) -> int {

}

func bar() -> float {
    
}

main () {
    x = 5;
    b = 6 + 10;
    c = 90 * 10 + 76;
    d = (70 + 10) * 90 + 60 + (80);
    z = c;
    f = b * c + d;
    print(hola)
    input(adios)
}

